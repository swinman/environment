{
  "$schema": "https://json-schema.org/claude-code-settings.json",
  "$comment": "SOURCE FILE. ~/.claude/settings.json is a symlink to this; edit here, never through the link, so changes are version controlled and reach the other machines.",
  "permissions": {
    "allow": [
      "Bash(source ~/software/environment/fpga_config.sh)",
      "Bash(git log *)",
      "Bash(git status)",
      "Bash(arm-none-eabi-size *)",
      "Bash(arm-none-eabi-nm *)",
      "Bash(arm-none-eabi-objdump *)",
      "Bash(lsusb)",
      "Bash(git push lucidsci claude-*)",
      "Bash(git push -u lucidsci claude-*)"
    ],
    "deny": [
      "Bash(aws)",
      "Bash(aws *)"
    ],
    "defaultMode": "auto"
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "~/software/luenv/claude_git_push_guard.sh",
            "statusMessage": "checking push destination"
          },
          {
            "type": "command",
            "command": "~/software/luenv/claude_aws_guard.sh",
            "statusMessage": "checking for direct aws use"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "a=$(jq -r 'try (.prompt | capture(\"^\\\\s*(?<a>gis|gdf)\\\\s*$\").a) // empty'); case \"$a\" in gis) git status >&2; exit 2;; gdf) git diff >&2; exit 2;; esac"
          }
        ]
      }
    ]
  },
  "enabledPlugins": {
    "clangd-lsp@claude-plugins-official": true
  },
  "tui": "fullscreen",
  "editorMode": "vim",
  "agentPushNotifEnabled": true,
  "autoMode": {
    "environment": [
      "### Org-wide",
      "**Organization**: None configured",
      "**Cloud provider(s)**: None configured",
      "**Repository visibility**: Not queryable via gh (origin remote host/owner not resolvable to a supported shape). Remotes are github.com:rabryan/environment.git and github.com:swinman/environment.git — assume private per shipped default until confirmed",
      "**Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary",
      "**Secrets management**: None configured",
      "**Default / protected branches**: Not queryable via gh (org/repo not derivable from origin remote)",
      "**CI/CD deploy targets**: None configured",
      "**Network posture**: None configured",
      "**Source control**: The trusted repo (github.com:rabryan/environment.git and github.com:swinman/environment.git remotes) only — no additional orgs configured",
      "**Trusted internal domains**: None configured",
      "**Trusted cloud buckets**: None configured",
      "**Key internal services**: None configured",
      "**Internal package registry**: None configured",
      "**Sensitive data locations & audiences**: any file or store holding personal data, confidential business data, credentials, regulated data, or similarly sensitive material; preserve exact handles when known and share only with audiences cleared at the [named+specifics] bar",
      "**Data retention / declassification**: None configured",
      "**Sensitive remote targets**: any namespace, host, or container whose name carries `prod` or `production` as a whole word or name segment (hyphen/underscore/dot-delimited)",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries `prod` or `production` as a whole word or name segment",
      "### User-specific",
      "**Primary use of Claude Code**: software development (personal dotfiles/environment configuration repo)",
      "**Trusted repo**: /home/winman/software/environment (this checkout), remotes github.com:rabryan/environment.git and github.com:swinman/environment.git — visibility unconfirmed, treat as private by default; confidential material is fine here but secrets/regulated data are never cleared in regardless of visibility",
      "**Org-specific CLIs**: None configured",
      "routine actions under this repo's setup/config_*.sh and dotfile paths are considered routine per CLAUDE.md working-style notes"
    ]
  }
}
